name: kernel
version: KERNELRELEASE
summary: Linex kernel
description: The upstream Linex kernel
grade: stable
confinement: strict
type: kernel

parts:
  kernel:
    plugin: kernel
    source: SRCTREE
    source-type: tar
    kernel-with-firmware: false
